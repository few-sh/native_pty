name: native_pty
description: A native pseudo-terminal library for Dart
version: 0.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.0.0

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  ffi: ^2.1.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.6
